# 2026-09-08（本会话：tests/06~07 收尾 + 任务 8/9/10）

## gopy 项目（tests/6.py~7.py 收尾 + 8/9/10 三任务推进）
- 接手状态：tests/01~05 + example.py 已与 python3 一致；tests/6.py 因限流中断、未编译验证。流程沿用：写 py → `python3` 取基线 → 改解释器 → `diff` 逐行比对，不修改测试文件。
- tests/6.py 收尾（字典方法 / 负数索引 / 切片步长 / pop·sort）：
  - `formatValue` 补 `case Tuple`，`d.items()` 输出 Python 风格 `('a', 1)`（此前打成 `[a 1]`）。
  - 普通索引分支补负数归一化 `if n < 0 { n += len(obj) }`，修 `nums[-1]` / `text[-1]` 求值失败、print 直接吐原文的问题。
  - 与 python3 基线一致；全量回归 01~06 + example 全绿。
- tests/7.py（类与对象 / try-except / import math）完整 TDD：
  - 类型：新增 `Instance{cls, fields}`、`ClassDef{name, methods, init, hasInit}`、`Module{name, funcs, consts}`；`flowKind` 加 `flowError`。
  - **编译坑**：`runtimeError` 未实现 `error` 接口，`i.lastErr = re` 报 `missing method Error`，补 `func (e runtimeError) Error() string`。
  - 类：`execStmts` 加 `class X:` 分支扫描体内 `def` 收集方法（`__init__` 置 hasInit）；实例化走 `default` 分支的 `*ClassDef`；`callMethodOn` 把 `self` 绑到实例并跳过 `self` 形参（非 self 参数按顺序对齐 args）。
  - 属性：新增"属性赋值"分支处理 `self.x = v` 写入 `Instance.fields`；`resolveToken` 加点号属性解析；`tokenize` 让 `obj.attr` / `math.floor` / `3.7` 保持为单 token（`.` 两侧为标识符或数字时不切分）。
  - 异常：除零改 `rtPanic`；`execStmts` 改具名返回 + `defer recover()`，把 `runtimeError` 转 `flowError`；`try:` 用独立 `execStmts` 子调用执行 try 体，收到 `flowError` 再执行 except 体；未捕获由顶层 recover 兜底不崩溃。
  - `import math`：新增 `mathModule()`，`*Module` 接入方法调用 switch。
  - **边界用例发现并修复**：`self.double() * 2` 得 6（丢掉 `* 2`）——调用分支把含 `(` 的整个表达式当一次调用，吞掉外围运算符。新增 `inlineCalls` + `callResultLiteral`，把参与运算的调用求值后替换为字面量再回落表达式引擎（整个表达式就是一次调用时不内联，避免与调用分支冲突 / 无限递归）。
- 任务 8/9/10（math·os 增强 / 类型转换 / 字符串 + `%` 格式化）开工，未收尾：
  - 字符串方法新增 `startswith/endswith/find/replace/count/isdigit/isalpha/isupper/islower/capitalize/title/lstrip/rstrip` + `titleCase` 辅助。
  - math 模块扩充 `ceil/pow/fabs/trunc/factorial/gcd/log/exp/degrees/radians/sin/cos/tan` 与常量 `e`；`gcd` / `factorial` 手写（不依赖 Go 1.22+ 的 `math.GCD`）。**修正**：误写不存在的 `toInt2`，改回 `toInt`。
  - 新增 `osModule()`：`getcwd()` / `listdir()` + 常量 `name="posix"`、`sep="/"`；`import os` 分支接入。
  - 内置函数：`str` 改用 `formatValue`（修 `str(3.0)` 吐 `3`、`str([1,2,3])` 吐 Go 风格 `[1 2]`）；`list` 增加字符串拆字符；新增 `int(x[,base])` / `long` / `float` / `repr` / `tuple` / `chr` / `ord` / `hex` / `oct` + `reprValue`（字符串加单引号对齐 Python repr）。
  - `%` 格式化：tokenize 运算符集合加 `%`，`prec` / `leftAssoc` 加 `"%": 5`（与 `*` `/` 同级）。
  - 未完成：`applyOp` 的 `%` 分支（数字取模 / 字符串格式化）、`inlinePercent` + `formatPercent`（printf 风格 `%s %d %x %f`）、元组字面量 `(a, b)` 解析；tests/08~10.py 未生成基线、未验证。
- 会话末态：`go build` 通过；项目已重构为 `main.go` / `lexer.go` / `parser.go` / `interpreter.go` 四文件（合计约 3015 行），测试文件统一为 `tests/01.py~15.py` 两位补零命名。README 已同步 tests/6、tests/7 的语法表 / 测试矩阵 / 实现要点。

# 2026-09-06

## gopy 项目
- 修复构建错误：`builtins.go` 中 `hex`/`oct`/`bin` 内置函数调用了未定义的 `formatRadix`，已补充该函数（按进制格式化整数，带 `0x/0o/0b` 前缀，支持负数）。
- 运行 `test.sh`，13 个测试 + example.py 全部与 python3 输出一致。
- 任务1（math/os 增强 + tests/13.py）验证通过，输出与 python3 逐字节一致。
- 任务2（类型转换/数值操作 + tests/14.py）：新增 `long` 内建（=int 支持 base），核对 int/long/float/str/repr/tuple/list/chr/ord/hex/oct；tests/14.py 通过。
- 任务3（字符串增强 + tests/15.py）：
  - 在 `objects.go` 的 `binaryOp` 的 `%` 分支实现 printf 风格字符串格式化（`%s %r %d %i %u %o %x %X %b %e %E %f %F %g %G %c %%`，支持宽度/精度/对齐/`#`/`+`/` ` 标志与 `%(name)s` 字典映射），新增 `padFieldA`/`fmtInt`/`fmtFloat`/`percentFormat` 等辅助函数。
  - 实现 `str.format`（自动编号/`{0}`/`{name}`、`!r`/`!s`、`:` 格式说明符），替换原 NotImplementedError 占位。
  - 新增字符串方法：`partition`/`rpartition`/`istitle`/`isnumeric`/`isdecimal`/`casefold`/`removeprefix`/`removesuffix`/`expandtabs`。
  - 注意：`newExc` 内部用 `fmt.Sprintf`，字面量 `%` 必须写成 `%%`，否则误判为格式动词导致 panic（已修正两处）。
- 全量回归：`tests/01~15.py` + example.py 全部与 python3 输出一致。README 已更新（str.format 从"尚未实现"移除，补充字符串方法说明）。
- 测试文件重命名为两位补零：`tests/1.py..15.py` → `tests/01.py..15.py`（`test.sh` 按 `tests/*.py` glob，仍全绿）。README 重写为更详细的设计/实现说明，并新增"测试目录"表格列出 01~15 + example 各自覆盖的测试点。

# 2026-09-08

## gopy 项目（10 轮 TDD 增强，每轮一次 commit，tests/16.py ~ tests/25.py）
- 流程：每轮先在 `tests/` 写 py 测试文件 → `python3` 取基准输出 → 修改解释器代码 → `diff` gopy 与 python3 输出逐字节一致 → 更新 README → `git commit`。最终 `tests/01~25.py` + example.py 全绿，代码量约 8300 → 10800 行。
- Round 1（tests/16.py，commit d52cd41）：
  - `nonlocal` 声明：`Environment` 加 `declaredNonlocal`，赋值绑定到最近拥有该名字的外层作用域，声明时校验存在性。
  - 装饰器：AST 加 `DecoratedStmt`，自内向外应用；类体中装饰结果为 `*Function` 进 Methods，其余（property 等包装）留作类属性。
  - 调用处 `*iterable`/`**mapping` 展开（`CallArg` 加 Star/Star2）；**发现并修正**：`*args` 收集应为 tuple 而非 list。
  - 函数 `__name__` 属性（getAttr 对 `*Function` 特判）。
- Round 2（tests/17.py，commit 6ce3afa）：
  - 生成器协程式实现：`Generator` 函数体跑在独立 goroutine，`yield` 在执行现场经无缓冲信道与 `next()` 调用方交替，`i.env`/`callDepth` 在每次交接时恢复调用方现场；`YieldStmt` 信号不向上传播。
  - 生成器表达式（含免括号实参 `sum(x for x in it)`）；`next`/`iter` 内置函数。
  - 包级初始化循环用钩子变量打破（`generatorIterateHook`、`genNextRef`）。
  - **修正**：耗尽生成器再迭代应返回空列表而非抛 StopIteration（`genNext` 耗尽返回 `!ok` 不带错，`next()` 负责转 StopIteration）。
- Round 3（tests/18.py，commit 94f1e6f）：
  - `with` 语句：多上下文项、按相反顺序调 `__exit__`、异常信息传入（exc_type 为 `PyType`，支持 `exc_type.__name__`）、返回 True 抑制异常；`PyType` 支持 `__name__`。
  - **bug**：抑制后仍返回原错误——execWith 需用 runWithExits 的返回值而非 berr。
- Round 4（tests/19.py，commit ae2570b）：
  - 运算符重载：`binaryOp`/`applyCompare`/`contains`/`truthy`/`getItem`/`setItem`/`callObject`/`len` 顶部优先走实例 dunder（算术含反射 `__r*__`，比较 `__eq__/__ne__/__lt__...`，另有 `__len__/__getitem__/__setitem__/__contains__/__call__/__bool__`）。
  - `__repr__`/`__str__` 分工修正为 CPython 语义：repr 用 `__repr__`，str 优先 `__str__` 回退 repr（`instanceStrHook` 加 dunder 参数）。
  - `@property`/`@staticmethod`/`@classmethod`（`unwrapClassAttr` 解包，classmethod 绑定动态类）；类属性赋值 `Cls.attr = v`。
  - **修正 Round 1 遗留 bug**：execClassDef 对非函数包装对象误删 classEnv 绑定。
- Round 5（tests/20.py，commit f17c800）：
  - `Class` 重构为 `Bases []*Class` + `MRO`（C3 线性化 `computeMRO`），LookupMethod/LookupAttr/isInstanceOf 全走 MRO。
  - 零参 `super()`：解释器加调用栈帧 `frames`，`Function` 加 `DefClass`；super 沿对象实际类型的 MRO 从当前类之后查找（菱形协作 `__init__` 正确只初始化一次）。`issubclass` 内置。
  - **修正**：`class D(B, C)` 多基类被 parseExpr 吃成元组 → parseClassDef 改用 parseConditional；classmethod 经子类/super 访问时应绑定访问处的动态类。
- Round 6（tests/21.py，commit 2524b1d）：
  - collections 模块：`Counter`（repr 按 most_common 排序、缺键返回 0、`most_common/elements/update/subtract`）、`defaultdict`（缺键调工厂并插入，工厂 repr 显示 `<class 'x'>`）、`OrderedDict`（repr 显示键值对列表、`move_to_end/popitem`）、`deque`（双端操作/rotate/remove/clear 等）。
  - **发现并修复**：`int()` 无参调用在 convertToInt 中越界 panic（CPython 返回 0）。
- Round 7（tests/22.py，commit c9c715f）：
  - functools：`reduce`/`partial`/`lru_cache`（`PyLRU` 缓存按参数 keyOf，`cache_info()` 输出 CacheInfo(hits=,misses=,maxsize=,currsize=)，可作装饰器或装饰器工厂）。
  - itertools 惰性迭代器家族 `PyIter`（Next 闭包）：`count/cycle/repeat/chain/islice/accumulate/product/permutations/combinations/takewhile/dropwhile/starmap`；`next` 支持 PyIter；无限迭代器被 `list()` 物化会挂起（测试规避）。
  - 括号 from-import `from x import (a, b)`。
- Round 8（tests/23.py，commit 890455a）：
  - 自定义异常类：`Class` 加 `IsException`/`ExcBase`；基类可为 `PyType` 异常；实例化为 `PyException{Cls, Args}`；`except` 按内建异常继承链（`excParents` 表）+ 用户类 MRO 匹配。
  - 异常实例即对象：`ValueError("x")` 返回值而非直接抛出（execRaise 处理 `*PyException` 值）；`e.args`、`str/repr`（KeyError 的 str 显示 key 的 repr）、`__cause__`、`type(e).__name__`。
  - `raise X from Y`（RaiseStmt 加 Cause）；try/finally（无 except）语法；except 元组已支持。
- Round 9（tests/24.py，commit b05a0eb）：
  - 字符串：`maketrans/translate`、startswith/endswith 元组参数（`prefixMatch`）、center/ljust/rjust 填充字符（`fillChar/padWith`）；`str.maketrans`、`dict.fromkeys` 经 getAttr 对 `*Builtin` 特判。
  - 运算符：集合 `| & - ^`（`setBinOp`，注意 `-` 走算术分支需在 switch 前拦截）、dict 合并 `|` 与 `|=`。
  - 内置：min/max `default`、`sum(start=)`、`format(v, spec)`（复用 applyFormatSpec）、`callable`。
- Round 10（tests/25.py，commit a7ab6e6）：
  - json 模块：`dumps`（indent/sort_keys/separators/ensure_ascii 含 `\uXXXX` 代理对、整数键转字符串、indent 模式分隔符变化）、`loads`（递归下降解析：对象/数组/字符串转义含 `\u` 代理对/整数浮点/true/false/null，尾部多余数据报错）。
  - **踩坑**：heredoc 生成 Go 代码时 `\\`/`"`/`\n` 转义被 shell/Python 多层转义弄乱，joinJSON 与 jsonString 需按行修复。
- 全量回归：`tests/01~25.py` + example.py 全部与 python3 输出一致；README 各轮同步更新（已支持语法、内置函数清单、模块清单、测试目录表格、"尚未实现"清单收敛为：关键字-only 参数、模块文件导入、`json.dump/load` 文件接口）。
- 下一轮候选方向：`yield from`/`.send()`/`.close()`、`@contextmanager`、文件 I/O（open + json.dump/load + csv）、字符串第三轮（f-string `=`、`int.bit_length`）、CPython 风格 traceback、关键字-only/位置-only 参数。
